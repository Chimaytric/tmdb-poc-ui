const ENVIRONMENT = 'localhost';
const PORT = '2000';

export const URL = `http://${ENVIRONMENT}:${PORT}`;

export const MOVIES_ENDPOINT = '/movies';
export const GENRES_ENDPOINT = '/genres';
export const SEARCH_ENDPOINT = '/search/';
